# Fieldnote 1.5.0 — Combined plan execution

## Batches landed (progressive)

- Plan: `docs/COMBINED_PLAN.md`
- Graph hygiene (prune/remap/unique ids)
- deleteItems + missing-dep satisfy
- Mind-map collapse fix + createMindMapTree
- HistoryEntry restores currentBoardId
- Task edit when done/blocked
- Nav overlay + draw onFinalize
- Live connector drag offsets
- Drawing body color → paths; shape stroke color
- ChromeLayoutProvider + disclosure + FloatingActionSheet scaffolds
