# Fieldnote improvements — v1.6.0 (Premium inventory)

## Program
Combined Plan v3 on `cursor/fieldnote-combined-plan-7fc8`. Batches 0–11 complete; Batch 12 = EAS + QA.

## Premium additions this release
1. Movable toolbar with grip, edge snap, persisted pose
2. Side resize handles (n/e/s/w) + corner handles
3. Edge-panning while dragging objects near viewport edges
4. Double-tap empty canvas → new Markdown note
5. Repair Map (layout normalize + tidy all mind-map roots)
6. Audio cards (`expo-av` soft) + playlist cards
7. Working folders with childIds
8. PDF cover treatment (coverUri + page-preview chrome)
9. Exhaustive `docs/COMBINED_PLAN.md` v3

## Prior
1.5.0–1.5.3 Combined Plan core, gesture pan default, Phase 2 guides/lasso/snapshots/lock/disclosure.
