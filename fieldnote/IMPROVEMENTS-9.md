# Fieldnote improvements — v1.5.2 (Combined Plan complete)

## Program status
Batches **0–12** of `docs/COMBINED_PLAN.md` are **done**. Patch releases:
- **1.5.0** — Combined Plan core
- **1.5.1** — pinch focal fix + one-finger pan default
- **1.5.2** — Phase 2 early (guides, lasso, snapshots, lock) + disclosure wiring

## This close-out
1. Exhaustive Combined Plan v2 (API sketches, per-file maps 0–12, chrome ASCII, DoD, QA)
2. Disclosure policy wired in `App.tsx` (`deriveChromeMode` / `visibleSlots`) — minimap hidden while editing
3. Exit region chip → chrome slot (`ExitRegionChip`)
4. Minimap uses `useChromeSlot` (no hard-coded top)
5. Paste AI **Merge** is primary; Replace secondary
6. Markdown shell overflow hidden (U19)

See `docs/COMBINED_PLAN.md` and `docs/QA_MATRIX.md`.
